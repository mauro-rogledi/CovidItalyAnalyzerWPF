<UserControl x:Class="WPFCovidItalyAnalizer.View.PieChartItalyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFCovidItalyAnalizer.View"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500" 
             d:DataContext="{d:DesignInstance local:PieChartItalyControl}">
    
    <Grid DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorLevel=1,AncestorType=UserControl}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#FFF3F3F3">
            <Label Content="Dal"
                   FontSize="14"
                   Margin="5"/>
            <DatePicker FontSize="14" Margin="5" SelectedDate="{Binding DateFrom, StringFormat=dd/MM/yyyy}" />
            <Label Content="al"
                   FontSize="14"
                   Margin="5"/>
            <DatePicker FontSize="14" Margin="5" SelectedDate="{Binding DateTo, StringFormat=dd/MM/yyyy}"/>
            <Label Content="Grafico"
                   FontSize="14"
                   Margin="5"/>
            <ComboBox ItemsSource="{Binding ChartDatas}"
                      SelectedValue="{Binding ChartSelected}"
                      Margin="5"
                      Width="220"
                      FontSize="14"></ComboBox>
            <Label Content="Max"
                   FontSize="14"
                   Margin="5"/>
            <ComboBox ItemsSource="{Binding Path=TopData }"
                      SelectedValue="{Binding TopSelected}"
                      Width="60" FontSize="10" Margin="5">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding display}" FontSize="14"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        <lvc:PieChart x:Name="PieChart" Grid.Row="1" 
                            FontSize="18"
                            FontWeight="Bold">
        </lvc:PieChart>
    </Grid>
</UserControl>
